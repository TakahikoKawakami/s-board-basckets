{% extends "base.pug" %}
{% block body %}
.container
    h1 分析一覧
    .card-container
        .card
            .card__image
            .card__title おむビー
            .card__summary お客様全体のデータから、取引の傾向を俯瞰します。
            a.card__link(href="/baskets/index")
        .card 
            .card__image comming soon...
            .card__title 商品ごとに詳しく
            .card__summary comming soon...
        .card 
            .card__card__image comming soon...
            .card__title 店舗ごとに詳しく
            .card__summary comming soon...


style.
    @media screen and (min-width:480px) { 
        .card-container {
            background-color: skyblue;
            display: grid;
            grid-template-columns: 1fr;  
        }
    }
    @media screen and (min-width:720px) { 
        .card-container {
            background-color: skyblue;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;  
        }
    }

    .card {
        width: 288px;
        height:350px;
        margin: 1.5em;
        //- padding: 0.5em;
        overflow: hidden;
        border-radius: 8px;
        border-left: solid 1px #ddd; 
        border-right: solid 1px #ddd; 
        border-bottom: solid 5px #888; 

        box-shadow: 0 1rem 1.0rem hsl(0 0% 0% / 20%);
        
        border: 1px solid hsl(0 0% 83%);
        position: relative;
    }

    .card__image {
        background: url(static/img/test.png) no-repeat center;        
        background-size: cover;
        box-sizing: border-box;
        width: 100%;
        height: 70%;
    }


    .card .summary {
        background-color: pink;
    }

    .card__link::after {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        z-index: 1;
        pointer-events: auto;
        content: "";
        background-color: transparent;
    }

{% endblock %}